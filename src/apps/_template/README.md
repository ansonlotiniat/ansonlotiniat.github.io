# __APP_LABEL__ (`__APP_ID__`)

Last updated: __DATE__

- Entry: `#__APP_ID__`; launcher labels/order/icon/shortcut: `manifest.json`.
- Content and controls: `view.html`, `controller.js`; optional local `content.json` or `content.js`.
- App layout: `styles.css`; window geometry: `window.css`.
- Assets: `assets/`; replace the authored placeholder icon and document any external sources here.
- State: in memory; close/minimize preserves it, reload resets it. No network or account access.
- Check: `npm run build && npm run check`; add behavior coverage for new interactions.
- Before release: update this README's purpose, state, asset sources, limitations, and the linked maintenance map.

See [App maintenance](../../../APP_MAINTENANCE.md), [architecture](../../../knowledge/architecture.md), and [map](../../../knowledge/map.md).
